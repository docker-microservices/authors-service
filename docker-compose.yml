services:
    authors-service: 
        image: "${DOCKER_USERNAME}/authors:latest"
        ports:
            - "8082:8082"
        networks:
          docker-ms:
            aliases:
              - authors
        deploy:
          replicas: 1
          restart_policy:
            condition: on-failure
          placement:
            constraints:
              - node.labels.role == apis
            
networks:
  docker-ms:
    external: true